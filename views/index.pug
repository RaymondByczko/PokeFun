extends layout

block content
  h1= title
  p Welcome to #{title}
  p(class="class_about") #{about}
  p(class="class_endpoints") #{endpoints}
  div(class="class_endpoints")
    -   for (var i = 0; i < endpoints.length; i++)
          div
            li= endpoints[i]
            button(class="button_fetch") Fetch
